# 外网 12 小时验收结果与容量处理

本轮 `validation-20260909-12h-004723` 完成北京时间 2026-09-09 00:47:23–12:47:25 的计划窗口，12:47:55 生成综合报告。冻结版本和启动验证见 [启动记录](external-acceptance-start-2026-09-09.md)。

**正确性、故障恢复和最终对账检查通过；综合报告仍为 failed，唯一未通过的底层检查是七天磁盘增长预测后的空间预留。原始报告和门槛保持不变。**

## 功能与故障结果

- 16 次故障动作全部通过，0 次动作失败，0 次基础设施恢复失败。包括缓存 Redis、持久 Redis、PostgreSQL、两个 DBProxy 实例、地图进程、动态地图回退、AOF 积压恢复和联合存储故障。
- 游戏完成 138 轮、276 个地图分片检查，其中 272 个健康、4 个不健康；不健康分片的结束时间均处于故障恢复窗口，后续恢复。结束时两个分片健康，角色身份违规和账号代际替换均为 0。动态地图丢失后原角色恢复至安全静态地图的检查通过。
- DBProxy 成功读取 5,133,947 次，入队 855,300 次，事务 426,589 次，交易 14,200 次。缺失快照、读取落后已确认 revision 和不变量错误均为 0，最终对账通过。
- 暂时错误保留：读取 4,889 次、入队 900 次、事务 1,612 次、交易 0 次。含错误的 11 个统计区间均与故障恢复窗口重叠；这是区间核对，不是逐请求因果定位，不宣称全程零错误。
- Relay 三段共提交 21,124 条事件，全部发布，0 死信；42,248 条快照、21,124 条事实及 Stream 唯一事件数量和顺序核对通过。Relay 重试共 331 次。
- 应用日志丢弃、指标重复、Prometheus 新增重复时间戳/乱序样本及日志捕获错误均为 0。
- 事后两个 DBProxy `/ready` 正常，缓存修复和 Outbox 均无待处理、处理中或死信记录，连接超限拒绝为 0。

## 容量门槛与归因边界

验收结束可用空间为 18,612,076,544 字节（约 17.3 GiB）。整个文件系统在窗口内净增长约 614 MiB；既有 Theil–Sen 估计预测七天增长约 11.19 GiB，剩余约 6.15 GiB，低于 12 GiB 预留门槛。

这是共享主机的整盘统计。期间另一应用有多次发布，数据库、观测存储及日志也共同使用磁盘。现有历史样本没有逐应用占用，无法把全部增长归给 DBProxy，也无法全部归给另一应用。一次发布产生的文件不能直接当作 DBProxy 的持续增长率。

## 已执行的容量处理

北京时间 13:45 清理历史构建的 31 个缓存目录，仅限旧 `target/debug` 和 `target/release/{deps,build,incremental}`。删除前确认目标真实路径、无 Rust 编译进程及无进程从目标目录运行；保留源码、发布二进制、数据库备份、回滚包和全部验收证据。

- 释放约 11.81 GiB，清理前可用 18,675,736,576 字节，清理后可用 31,352,737,792 字节（约 29.2 GiB）。实际数值含同期其他进程的少量变化。
- 保留的 21 个发布二进制在清理前后 SHA-256 一致；回滚包、数据库备份和已部署二进制校验通过。
- 未清理数据库卷或另一应用的数据，未重启游戏或 DBProxy 服务。
- 即使沿用旧的七天增长估算，清理后的算术余量也约为 18 GiB，超过 12 GiB 门槛。这只是容量复核，不回写旧报告，也不表示新的七天或 12 小时验收通过。

清理明细和哈希位于部署证据目录的 `cache-cleanup.json`、`cache-cleanup.log`、`backup.sha256` 和 `original-acceptance-final.sha256`。

## 分项采样与后续判断

已部署 [目录空间采样工具](../tools/sample_disk_usage.py)，由 `tiangz-disk-usage.timer` 每 15 分钟执行。首次采样于北京时间 13:46 完成，11 个分组无采样错误；随后再次执行验证追加写入，共两条相同配置的有效样本、0 采样错误。配置位于 `/etc/tiangz/disk-usage.json`，输出为 `/var/log/tiangz-disk-usage/samples.jsonl`。

分组分别记录 DBProxy PostgreSQL、可靠 Redis、观测数据、另一应用文件及数据库、历史构建与备份、运行日志和验收证据、已部署程序、系统 journal、容器镜像及容器日志。整盘 used/available 同时保留，原有验收门槛未调整。

工具使用 `du -x -s -B1` 记录实际分配空间，失败路径写入 error 并返回非零退出码；文件锁避免并发采样。日志约 8 MiB 轮转，保留两份历史文件。配置包含 `groups`（组名到绝对目录数组）、`filesystem` 和 `context`，每条样本记录配置哈希，变更配置后应重新建立可比基线。目录应不重叠；硬链接、文件系统元数据和未覆盖目录使各组不能简单加总替代 `df`。

Linux 使用示例：

```sh
python3 tools/sample_disk_usage.py --config /etc/tiangz/disk-usage.json \
  --output /var/log/tiangz-disk-usage/samples.jsonl
```

后续至少观察一天的分类增量，并对照发布、备份和负载时间区分一次性占用与持续增长。目前采样明确标记为验收负载已停止的共享主机状态；该基线不能直接外推完整压测负载。仅在空间余量不足、出现不明持续增长或需声明持续负载容量时，再进行容量专项验证。

最终报告与容量处理证据另存为 `final-and-capacity-evidence.tar.gz`，SHA-256 为 `f686a45a8300dabdd880f83cb5611f8af43536c799a92b31f114e21fa68e0259`。本次只增加运维采样工具和文档，已验证实际采样、追加写入、systemd 配置及文档空白检查；没有重跑 Rust 测试或修改运行中的业务二进制。
