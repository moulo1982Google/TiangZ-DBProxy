# 缓存清理、批次提交与存储路径验收

**最终结果：NVMe PG 路径下 r6 完整 30 分钟、五次故障恢复、最终对账和独立审计全部通过。机械盘路径 r4 仍保留为失败，不能将本结论外推为任意磁盘延迟下均能满足游戏 5 秒 RPC。** 产品修复、存储路径和负载工具调整的证据分别记录如下。

2026-09-07。产品修复分别为 `24d66b7`（提交后缓存清理由既有维护 worker 有界批量处理）和 `4432808`（同一 PG 的 SaveMulti 批次一次提交）。两项都有旧实现失败、新实现通过的精确回归，不能由此推断本机 100 玩家全程验收已经通过。

## 机械盘路径的结果

- `20260907-cleanup-30m-r1`：清理候选启动通过，约第 18 分钟正常阶段游戏 RPC 超时，未完成 AOF/最终对账。
- `20260907-cleanup-batch-30m-r2`：前置真实测试等待 Redis 健康的约 10 秒窗口过短，未开始计时。测试等待改为覆盖既有 Compose 健康检查窗口的 70 秒。
- `20260907-cleanup-batch-30m-r3`：批次候选正常阶段通过，约第 23 分钟 AOF 重启仍在加载，控制器固定等待 10 秒后探测收到 BusyLoading，未最终对账。TiangZ `e6dfcc2` 改用既有 90 秒窗口等待所需 Redis 的真实健康状态；独立专项实际等待 18,798 ms 后就绪。原失败轮次及专项新轮次各 64 条已确认记录事后恢复通过，原轮次仍保留为失败。
- `20260907-cleanup-batch-30m-r4`：冻结 DBProxy `e364361` / TiangZ `e6dfcc2`，前置 139 项默认、33 项真实数据库/故障及 7 项控制器测试通过。北京时间 20:13:18 开始，20:34:26 因正常阶段地图 100 的 1 次探针和 1 次移动超时失败，业务 RPC 期限仍为 5 秒。cache、PG、可靠 Redis 三次恢复通过，PG 恢复有 7 次登录重试。最后分钟统计读取 114,213 次，读取错误 4、入队错误 300、事务错误 810、交易错误 19；旧读、缺失、不变量错误均 0。读取错误及 1 次新增事务错误发生在正常阶段，不能全部归入故障窗口。没有 AOF 强杀和最终对账，不能记作 30 分钟通过。

r4 失败前约 44 秒，主 DBProxy 提交后缓存同步均值约 5.3 ms，PG 排队约 428.5 ms、执行约 242.0 ms；两张地图 SDK 排队约 595/384 ms、交换约 777/766 ms。指标是相邻计数器差值，非 p99 相减；PG 执行不等同于纯 SQL 或 fsync。缓存清理已不再是该窗口的主要等待，但 PG 与 SDK 仍存在长尾。周期快照持有玩家有序 mailbox，底层等待会阻塞该玩家后续消息；连接排队预算不包含 SQL 执行，不能凭排队有界保证整个业务 RPC 低于 5 秒。

## 隔离存储路径对照

Docker 默认数据 VHD 位于 F 盘机械硬盘，D 盘为 NVMe。相同 PostgreSQL 18.4 `pg_test_fsync` 的单次 8 KB fdatasync，旧路径约 54 ms，D 盘专用 bind 路径约 1.06 ms。该对照同时改变设备和文件系统访问路径，不能直接宣称纯硬盘性能倍数，也不证明突然断电时的物理持久性。

两客户端、30 秒、相同单条 INSERT 事务对照，均确认 `fsync=on`、`synchronous_commit=on`、`wal_sync_method=fdatasync`：NVMe 路径 27,423 次成功、0 失败，平均 2.187 ms、914.4 TPS；机械盘路径 537 次成功、0 失败，平均 112.498 ms、17.78 TPS。该小事务对照支持先验证部署存储路径，不能当作完整游戏吞吐结论。

新增 `deploy/local/docker-compose.validation-postgres-bind.yml` 仅在显式叠加时替换本项目 PG 的数据挂载。必须设置 `DBPROXY_VALIDATION_PG_DATA_DIR` 为既有独立目录，保留 PG 18 父目录挂载、原镜像、资源限制、fsync、synchronous_commit、WAL 设置、连接数、Redis AOF/WAITAOF、负载和业务超时。可靠 Redis 实际为 AOF `everysec`，可靠写入额外使用 `WAITAOF 1 0 2000`，不是依赖 `appendfsync always`。缓存仍是无持久化 tmpfs。

对照不会迁移 Docker Desktop 全局数据，也不操作其他项目。原 PG named volume 留存；新路径只含演练数据，不复制原 `tiangz` 数据库。用原三个 Compose 文件、不叠加 bind 文件，执行 `up -d --no-deps --force-recreate postgres` 即可回到原卷；禁止同时让两个 PG 实例使用同一目录。新路径必须重新通过真实测试、100 玩家启动、完整 30 分钟及最终对账，结果待补。

原始证据位于工作区 `.build-tmp/local-validation/` 各唯一轮次及 `.build-tmp/repair-ack-20260907/`，不覆盖失败文件，不提交配置凭据或数据库文件。TiangZ 控制器修复后 `verify:quick` 23 步通过，codegen 无 Git 差异；DBProxy 格式、严格 Clippy 通过。七天长稳、完整发布门禁与 WoW335 实机验收未执行。

## NVMe 首轮仍未完成

`20260907-cleanup-batch-nvme-30m-r5` 冻结 DBProxy `78ebf31` / TiangZ `dda76a5`，与 r4 的全部冻结二进制、脚本、配置及资源哈希相同，仅 PG 数据挂载改变。前置测试通过，北京时间 20:50:00 开始，21:09:00 在地图 1 的探针建连阶段收到 Windows 10055 后失败，未进入 AOF/最终对账。此前正常阶段完成的游戏检查全部合格；最后分钟统计读取 131,502 次，读取错误、旧读、缺失、不变量错误均 0；入队错误 236、事务错误 723、交易错误 100，PG 恢复有 3 次登录重试。不能将这轮写成 30 分钟通过。

失败现场可用物理内存约 13.1 GiB、最近 CPU 约 3.4%。事后动态 TCP 范围仍为 49,152–65,535，连接数也不足以证明系统端口池耗尽；10055 的具体内核原因尚未确定。探针已有按显式 loopback 源地址分配固定源端口的 Windows 功能，控制器原先未传入 `--source-ip`。现仅在 Windows 按轮次和地图传入独立源地址，并记录 `game_probe_source`；不新增连接或业务重试，不改变失败判定。独立 OS 诊断在其他 loopback 地址完成 400 次连接、0 错误，只证明源地址绑定可用，不能替代完整重跑。

## 最终 r6 验收

- 冻结候选：DBProxy `0114af0`、TiangZ `d7af251`，两仓库工作区均干净；产品二进制沿用 r4/r5，TiangZ 仅增加 Windows 负载工具源地址隔离。35 个冻结文件最终 SHA256 全部匹配，130 个源地址均唯一。
- 唯一轮次：`20260907-cleanup-batch-nvme-30m-r6`。北京时间 **21:17:52.248–21:47:52.248** 为计划负载，**21:48:20.361** 最终对账通过，控制器退出码 0，**21:48:31.915** 独立只读审计通过。
- 负载保持 100 游戏玩家、独立 100 持久化玩家及 Relay；PG 请求连接 2 条、游戏 SDK 池 2 条、业务 RPC 5 秒、PG 排队 2 秒、缓存操作 200 ms 均未调整。PG 使用独立 NVMe bind，可靠 Redis 保持机械盘 AOF 卷和 WAITAOF 确认。
- cache / PG / reliable Redis / AOF / cache 五次故障全部恢复；每次要求同账号连续两轮游戏检查和交易检查。PG 恢复有 **3 次登录重试**，不可省略。124 个已完成地图检查的探针、移动、业务传输错误均为 0；未完成的恢复登录轮次由上述重试单独记录。
- 成功读取 **206,742** 次；低于已确认 revision 的旧读、缺失、不变量错误均 **0**。暂时错误保留：读取 **2**、入队 **364**、事务 **875**、交易 **100**；读取错误发生于 PG 故障/恢复窗口，正常阶段未新增。普通事务成功 **16,321** 次，持久化探针交易成功 **1,300** 次，入队成功 **34,636** 次。
- AOF 强杀前后队列总数均为 **164**（164 pending → 100 pending + 64 processing），专用 **64 条已 ACK 记录全部落库验证通过**。
- Relay **819** 次提交，SQL published **819**、dead **0**，实际 Stream 唯一事件 **819**；双记录快照 **1,638/1,638** 正确，不可变事实 **819**。真实游戏交易审计与发布 **2/2**，实际交易 Stream 覆盖全部事件。Relay 重试 **42** 次；这是至少一次投递，最终按 event_id 去重及顺序对账。
- 无丢弃日志或永久契约错误。可用物理内存最低 **8.873 GiB**，全机 CPU 采样峰值 **81.42%**，最长采样间隔 **7,649 ms**；有一次资源压力快照且采集成功，未触发既有资源失败门限，不宣称全程低 CPU。

正常窗口的相邻计数器统计（不跨故障恢复区间，分桶 p99 非精确分位数）：

| 阶段 | 样本数 | 均值 | p99 所在桶 |
|---|---:|---:|---:|
| 主 DBProxy PG 排队 | 11,765 | 2.71 ms | (50, 100] ms |
| 主 DBProxy PG 执行 | 11,768 | 16.89 ms | (100, 250] ms |
| 主 DBProxy 提交后缓存同步 | 10,684 | 0.75 ms | (1, 5] ms |
| 地图 1 SDK 排队 / 交换 | 1,506 / 1,506 | 3.84 / 24.90 ms | (10, 50] / (100, 500] ms |
| 地图 100 SDK 排队 / 交换 | 1,506 / 1,506 | 3.94 / 25.81 ms | (10, 50] / (100, 500] ms |

采样没有计数器重置或缺失阶段。正常阶段各轮业务 p99 最大值 **261.261 ms**，最后一轮两图分别 **11.941/11.554 ms**；这些不是所有请求合并后的 p99。r4 的失败窗口与 r6 的全部正常窗口不是等长配对样本，不能直接相除宣称代码加速倍数。

最终前置仍为 139 项默认、33 项真实数据库/故障、7 项控制器测试通过；控制器两次修改后 `verify:quick` 均 23 步通过，DBProxy 格式/严格 Clippy 通过，生成物无 Git 差异，既有 LNK4098 警告保留。清理检查确认所有受管游戏/DBProxy 服务端口释放、三只数据库容器健康、原机械盘 named volume 留存；当前本机测试 PG 保持在 NVMe 目录。其他项目、Docker Desktop 全局数据和外网部署未改动。

该验收证明本机此配置的恢复与数据正确性，不替代机械盘容量保证、突然断电测试、跨机高可用、完整发布门禁、七天长稳或 WoW335 实机验收。Windows 10055 在 r6 未复现，其具体 OS 根因仍未确定。完整失败与成功证据均保留，审计入口为 r6 的 `audit.json`、`acceptance-summary.json`、`latency-analysis.json` 和 `cleanup.json`。

证据 SHA256 索引为 `.build-tmp/repair-ack-20260907/evidence-sha256.json`，覆盖六个唯一轮次及本轮构建/诊断的 **977 个文件、770,575,426 字节**，排除凭据、活跃 PG 数据目录和 fsync 临时文件；旧轮次索引没有覆盖或改写。该索引不随 Git 提交，迁移时须另外保存受控证据目录，不能公开打包凭据或数据库文件。
